services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: msm_app
      POSTGRES_USER: msm
      POSTGRES_PASSWORD: msm_dev_password
    ports:
      - "5432:5432"
    volumes:
      - msm_pgdata:/var/lib/postgresql/data

volumes:
  msm_pgdata:
